export const publicRoutes = ['/', '/about', '/our-services', '/contact-us']

export const authRoutes = ['/sign-in', '/sign-up']

export const apiAuthPrefix = '/api/auth'

export const DEFAULT_LOGIN_REDIRECT = '/dashboard'
