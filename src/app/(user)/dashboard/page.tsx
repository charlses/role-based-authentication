import { auth } from '@/auth'
import { ClientDashboard } from '@/components/user/ClientDashboardPage'

const DashboardPage = async () => {
  return <ClientDashboard />
}

export default DashboardPage
